name: HyperAI CI

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Hello from remote CI
        run: |
          echo "Repository ${{ github.repository }}"
          echo "Branch ${{ github.ref_name }}"
          echo "Remote CI is reachable"
