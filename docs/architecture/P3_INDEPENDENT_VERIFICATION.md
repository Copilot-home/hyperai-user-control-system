# P3 — Independent Verification Boundary

Observation -> Independent Checker -> PASS/FAIL/UNKNOWN

The checker is read-only and has no state or commit authority. UNKNOWN never becomes PASS.

Production browser verification remains a separate live-runtime gate.
